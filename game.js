export default class Game {
    constructor(title, category, price, description, imageFile) {
      this.title = title;
      this.category = category;
      this.price = price;
      this.description = description;
      this.imageFile = imageFile;
    }
}